/* ==================================
   re-section.js - Monthly Retrospective Section
   ⚠️ 이 파일은 manager.py가 자동 생성
   ================================== */

const isLocal = window.location.protocol === 'file:';

// Re section data - Generated by manager.py
const reData = [{"month": "2026.09", "url": "#", "title": "다시 찾아오는 새벽녗 속 반짝이는 눈빛을 떠나가는 달빛에게 투사시키며.", "title_en": "Projecting the glittering gaze within the returning dawn onto the departing moonlight.", "content_file": "2026_09_Resolve.json", "type": "Resolve"}, {"month": "2026.08", "url": "#", "title": "청완하게 떨어지는 유성을 맞이하던 삼경을 지나, 다시 찾아온 새벽녘을 맞이하며.", "title_en": "Past the third watch of falling meteors, greeting the dawn that has returned.", "content_file": "2026_08_Retrospect.json", "type": "Retrospect"}, {"month": "2026.08", "url": "#", "title": "밤하늘에 떨어지는 31개의 유성 아래 두 손을 모아 눈을 감고 언젠가 맞이 할 그 날을 고대하며.", "title_en": "Beneath 31 falling meteors, hands clasped and eyes closed, awaiting the day I will someday greet.", "content_file": "2026_08_Resolve.json", "type": "Resolve"}, {"month": "2026.07", "url": "#", "title": "광원을\b관측하던 망원경 뒤, 찬연하는 눈빛으로 광휘했던 7월을 떠나보내며.", "title_en": "Behind the telescope that watched the light, bidding farewell to July that shone with radiant eyes.", "content_file": "2026_07_Retrospect.json", "type": "Retrospect"}, {"month": "2026.07", "url": "#", "title": "떠나가는 6월의 그림자 뒤, 발광하며 다가오는 7월을 관측하며.", "title_en": "Observing July approaching, glowing, behind the departing shadow of June.", "content_file": "2026_07_Resolve.json", "type": "Resolve"}, {"month": "2026.06", "url": "#", "title": "건네본 안녕 끝에 유월하게 될 유월을 떠나보내며.", "title_en": "At the end of the hellos I offered, bidding farewell to June as it now passes over.", "content_file": "2026_06_Retrospect.json", "type": "Retrospect"}, {"month": "2026.06", "url": "#", "title": "유월한 5월에게 안녕을 말하고, 찾아온 유월에게 안녕을 건네며.", "title_en": "Saying goodbye to May that crossed over, and greeting June as it arrives.", "content_file": "2026_06_Resolve.json", "type": "Resolve"}, {"month": "2026.05", "url": "#", "title": "오월이라는 이름의 겨냥하던 과녁을 유월한 31발의 총성을 6월에 유월하며.", "title_en": "Passing the 31 gunshots that targeted May's bullseye over to June.", "content_file": "2026_05_Retrospect.json", "type": "Retrospect"}, {"month": "2026.05", "url": "#", "title": "관조하던 사월의 사계를, 오월이라는 이름으로 겨냥하며.", "title_en": "Targeting the seasons of April I contemplated, under the name of May.", "content_file": "2026_05_Resolve.json", "type": "Resolve"}, {"month": "2026.04", "url": "#", "title": "넥스트 러너로서의 발걸음을 잠시 붙잡아, 지나갈 사월을 관조하며.", "title_en": "Pausing the steps as a \"Next Runner,\" contemplating the passing April.", "content_file": "2026_04_Retrospect.json", "type": "Retrospect"}, {"month": "2026.04", "url": "#", "title": "건네받은 삼월의 나날들, 사월이라는 이름표와 넥스트 러너로서 달려나가며", "title_en": "Passing over the days of March, with a name tag called April, running as a Next Runner", "content_file": "2026_04_Resolve.json", "type": "Resolve"}, {"month": "2026.03", "url": "#", "title": "궤도에 Merge 시킨 나날들을 사월의 동력으로 건네며", "title_en": "Passing the Days Merged into Orbit as Fuel for April", "content_file": "2026_03_Retrospect.json", "type": "Retrospect"}, {"month": "2026.03", "url": "#", "title": "3월을 궤도에 Merge 시키며", "title_en": "Merging March into Orbit", "content_file": "2026_03_Resolve.json", "type": "Resolve"}, {"month": "2026.02", "url": "#", "title": "2월을 26시키며.", "title_en": "Landing February 2026.", "content_file": "2026_02_Retrospect.json", "type": "Retrospect"}, {"month": "2026.02", "url": "#", "title": "1월의 노력을 2월에 이월하며 :)", "title_en": "Carrying Over January's Efforts into February :)", "content_file": "2026_02_Resolve.json", "type": "Resolve"}, {"month": "2026.01", "url": "#", "title": "2026년 1월을 떠나보내며.", "title_en": "Farewell to January 2026.", "content_file": "2026_01_Retrospect.json", "type": "Retrospect"}, {"month": "2026.01", "url": "#", "title": "2026년을 맞이하며 (Welcoming 2026)", "title_en": "Welcoming 2026", "content_file": "2026_01_Resolve.json", "type": "Resolve"}];

let activeFilter = 'all';
let activeReKey = null;

function generateReCards() {
    const grid = document.getElementById('reGrid');
    const lang = typeof currentLang !== 'undefined' ? currentLang : 'EN';
    
    const filteredData = activeFilter === 'all' 
        ? reData 
        : reData.filter(item => item.month.startsWith(activeFilter));

    grid.innerHTML = filteredData.map((item, idx) => `
        <div class="re-card" onclick="openReModal(${idx})" data-idx="${idx}">
            <div class="re-month">${item.month}</div>
            <div class="re-title">${lang === 'KR' ? item.title : (item.title_en || item.title)}</div>
            <div class="re-status">${lang === 'KR' ? '클릭하여 읽기' : 'Click to read'}</div>
        </div>
    `).join('');
    
    updateFilterOptions();
}

function updateFilterOptions() {
    const filter = document.getElementById('reYearFilter');
    if (!filter) return;
    
    const years = [...new Set(reData.map(item => item.month.split('.')[0]))].sort().reverse();
    const currentOptions = Array.from(filter.options).map(opt => opt.value);
    
    years.forEach(year => {
        if (!currentOptions.includes(year)) {
            const option = document.createElement('option');
            option.value = year;
            option.textContent = year + ' Year';
            filter.appendChild(option);
        }
    });
}

function filterReCards() {
    const filter = document.getElementById('reYearFilter');
    activeFilter = filter.value;
    generateReCards();
}

function openReModal(idx) {
    const lang = typeof currentLang !== 'undefined' ? currentLang : 'EN';
    const item = activeFilter === 'all' ? reData[idx] : reData.filter(d => d.month.startsWith(activeFilter))[idx];
    
    if (!item) return;
    activeReKey = item.month + '|' + item.type;

    const overlay = document.getElementById('reModalOverlay');
    const contentArea = document.getElementById('reModalContent');
    const titleText = lang === 'KR' ? item.title : (item.title_en || item.title);
    
    // Show loading state
    contentArea.innerHTML = `
        <div class="re-modal-month">${item.month}</div>
        <h2 class="re-modal-title">${titleText}</h2>
        <div class="re-modal-body" style="text-align: center; padding: 40px 0;">
            <div class="loading-spinner">Loading...</div>
        </div>
    `;
    
    overlay.classList.add('show');
    document.body.style.overflow = 'hidden';

    // Fetch the content (cache: 'no-store' so language switches don't keep stale KR/EN JSON)
    const contentPath = 'assets/re_data/' + item.content_file;
    
    fetch(contentPath, { cache: 'no-store' })
        .then(response => {
            if (!response.ok) throw new Error('File not found');
            return response.json();
        })
        .then(data => {
            const textContent = lang === 'KR' ? (data.desc_kr || data.desc) : (data.desc_en || data.desc || data.desc_kr);
            
            contentArea.innerHTML = `
                <div class="re-modal-month">${item.month}</div>
                <h2 class="re-modal-title">${titleText}</h2>
                <div class="re-modal-body">
                    ${textContent}
                </div>
                ${(!isLocal && item.url && item.url !== '#') ? `<a href="${item.url}" target="_blank" class="re-link" style="margin-top: 40px; display: inline-block;">Read full review →</a>` : ''}
            `;

            // Re-render MathJax inside the modal
            if (typeof MathJax !== 'undefined' && MathJax.typesetPromise) {
                MathJax.typesetPromise([contentArea]).catch(err => console.log('MathJax:', err));
            }
        })
        .catch(err => {
            console.error('Error loading review:', err);
            contentArea.innerHTML = `
                <div class="re-modal-month">${item.month}</div>
                <h2 class="re-modal-title">${titleText}</h2>
                <div class="re-modal-body" style="color: #ff6b6b; text-align: center;">
                    ${lang === 'KR' ? '내용을 불러올 수 없습니다.' : 'Failed to load content.'}
                </div>
            `;
        });
}

function closeReModal(event) {
    const overlay = document.getElementById('reModalOverlay');
    overlay.classList.remove('show');
    document.body.style.overflow = 'auto'; // Restore background scrolling
    activeReKey = null;
}

function refreshOpenReModal() {
    const overlay = document.getElementById('reModalOverlay');
    if (!overlay || !overlay.classList.contains('show') || !activeReKey) return;

    const [month, type] = activeReKey.split('|');
    const filteredData = activeFilter === 'all'
        ? reData
        : reData.filter(item => item.month.startsWith(activeFilter));
    const idx = filteredData.findIndex(item => item.month === month && item.type === type);
    if (idx >= 0) openReModal(idx);
}

// Add Escape key listener to close modal
document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
        const overlay = document.getElementById('reModalOverlay');
        if (overlay && overlay.classList.contains('show')) {
            closeReModal();
        }
    }
});
